{% extends 'base.html.twig' %}

{% block body %}

        <div>aantal is  {{ cats|length }} </div>



        <div class="row gy-3">
            {% for cat in cats %}
                <div class="col-sm-3">
                    <div class="card">
                        <div class="card-body text-center">
                                                   <a href="{{ path('pizza_categories', {id: cat.id})  }}">
                            <img alt="no picture" class="product-img img-fluid center-block" src="{{ asset(cat.image) }}">
                                                  </a>
                            <div class="card-title mb-3">{{ cat.name }}</div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
        {#    <div class="row">#}
        {#        {% for cat in cats %}#}
        {#            <div class="col-3">#}
        {#                <li>{{ cat }}</li>#}
        {#            </div>#}
        {#        {% endfor %}#}
        {#    </div>#}

    </div>
{% endblock %}
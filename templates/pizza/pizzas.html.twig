{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <header>
        <h1 class="display-4">Pizza Winkel</h1>
    </header>
    <img src="{{ asset('img/pizzabanner.jpg')}}" class="img-fluid"  width="1500 px" alt="...">



    <div class="row gy-3">
        {% for pizza in pizzas %}
            <div class="col-sm-3">
                <div class="card">
                    <div class="card-body text-center">
{#                        <a href="{{ path('pizza_categories') }}">#}
                            <img alt="no picture" class="product-img img-fluid center-block" src="{{ pizza.image }}">
{#                        </a>#}
                        <div class="card-title mb-3">{{ pizza.name }}</div>
                        <div class="card-title mb-3">{{ pizza.price }}</div>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
{#    <div class="row">#}
{#        {% for cat in cats %}#}
{#            <div class="col-3">#}
{#                <li>{{ cat }}</li>#}
{#            </div>#}
{#        {% endfor %}#}
{#    </div>#}

</div>
{% endblock %}
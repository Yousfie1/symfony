{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <header>
        <h1 class="display-4">Pizza Winkel</h1>
    </header>
    <img src="{{ asset('img/pizzabanner.jpg')}}" class="img-fluid"  width="1500 px" alt="...">
    <div>aantal is  {{ cats|length }} </div>



    <div class="row gy-3">
        {% for cat in cats %}
            <div class="col-sm-3">
                <div class="card">
                    <div class="card-body text-center">
{#                        <a href="{{ path('pizza_categories') }}">#}
                            <img alt="no picture" class="product-img img-fluid center-block" src="{{ asset('img/vispizza.png') }}">
{#                        </a>#}
                        <div class="card-title mb-3">{{ cat }}</div>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}